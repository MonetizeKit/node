name: Release
on:
  push:
    branches: [main]
concurrency: release-${{ github.ref }}
jobs:
  release:
    uses: MonetizeKit/.github/.github/workflows/reusable-sdk-release.yml@v1
    permissions:
      contents: write
      pull-requests: write
      id-token: write
    secrets: inherit
